name: secure_ble_chat
version: 1.0.0
description: A secure Bluetooth chat application.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.0.0
  encrypt: ^5.0.0
  flutter_riverpod: ^2.0.0
  cupertino_icons: ^1.0.0
  http: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true